# ─── ADMIN ─────────────────────────────────────────────────────
# Credenciais para aceder ao painel /admin
# IMPORTANTE: alterar antes de fazer deploy
ADMIN_USERNAME=admin
ADMIN_PASSWORD=mab2026

# Chave secreta usada para assinar a sessão (HMAC-SHA256).
# Gerar uma string aleatória longa (mínimo 32 caracteres):
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
ADMIN_SESSION_KEY=CHANGE-THIS-TO-A-LONG-RANDOM-STRING-AT-LEAST-32-CHARS

# ─── AMBIENTE ──────────────────────────────────────────────────
NODE_ENV=production
